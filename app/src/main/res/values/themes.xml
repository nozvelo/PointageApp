<resources>
    <!-- On reste sur AppCompat pour ne pas toucher aux dépendances -->
    <style name="Theme.PointageApp" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/md_bg</item>
        <item name="android:navigationBarColor">@color/md_bg</item>
        <item name="android:colorBackground">@color/md_bg</item>

        <!-- Couleurs primaires pour compat -->
        <item name="colorPrimary">@color/md_primary</item>
        <item name="colorPrimaryDark">@color/md_primary_dark</item>
        <item name="colorAccent">@color/md_primary</item>

        <item name="android:textColor">@color/md_text</item>
        <item name="android:fontFamily">sans-serif</item>
        <item name="android:buttonStyle">@style/Widget.PointageApp.Button.Primary</item>
    </style>

    <!-- Styles texte -->
    <style name="Text.Welcome" parent="@android:style/TextAppearance.Material.Headline">
        <item name="android:textColor">@color/md_text</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">26sp</item>
    </style>

    <style name="Text.Status" parent="@android:style/TextAppearance.Material.Body1">
        <item name="android:textColor">@color/md_text_muted</item>
        <item name="android:textSize">14sp</item>
    </style>

    <style name="Text.Chrono" parent="@android:style/TextAppearance.Material.Display1">
        <item name="android:textColor">@color/md_text</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">32sp</item>
    </style>

    <!-- Boutons -->
    <style name="Widget.PointageApp.Button.Primary" parent="@android:style/Widget.Button">
        <item name="android:background">@drawable/btn_primary_bg</item>
        <item name="android:textColor">@color/md_on_primary</item>
        <item name="android:minHeight">56dp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:paddingLeft">16dp</item>
        <item name="android:paddingRight">16dp</item>
    </style>

    <style name="Widget.PointageApp.Button.Secondary" parent="@android:style/Widget.Button">
        <item name="android:background">@drawable/btn_secondary_bg</item>
        <item name="android:textColor">@color/md_text</item>
        <item name="android:minHeight">48dp</item>
        <item name="android:paddingLeft">16dp</item>
        <item name="android:paddingRight">16dp</item>
    </style>
</resources>
